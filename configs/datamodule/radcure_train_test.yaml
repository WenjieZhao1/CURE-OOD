_target_: src.datamodules.radcure_datamodule_train_test.RadCureDataModule


root_dir: ${original_work_dir}
data_dir: ${data_dir} # data_dir is specified in config.yaml
# Set train_filename and test_filename to the corresponding cohort CSV paths.
train_cache_dir: ${data_dir}/nifti_cache
test_cache_dir: ${data_dir}/nifti_cache

train_filename: ${data_dir}/ExposureTime_Train.csv
test_filename: ${data_dir}/ExposureTime_ID.csv

Fold: 1
patch_xy: 80 # whole image
patch_z: 48 # whole image
patch_sz: 8 # patch for vit
batch_size: 32
dataset_mean: 0.1209 
dataset_std: 0.7184
#train_val_test_split: [176, 24, 24]
num_workers: 8
pin_memory: True
p_tumor: 0.5

time_bins_data: ${model.time_bins}
